@model IEnumerable<project_webbservice.Models.UserListAudio>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserList)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Audio)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserList.ListName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Audio.Title)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.UserListAudioID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.UserListAudioID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.UserListAudioID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
</body>
</html>
